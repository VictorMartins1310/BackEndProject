spring:
  datasource:
    url: jdbc:postgresql://db.nltqgtftdcbzdbgubmoa.supabase.co:6543/postgres
    driver-class-name: org.postgresql.Driver
  hikari:
    max-lifetime: 180000   # 3 Minuten
    idle-timeout: 120000   # 2 Minuten
    minimum-idle: 1
    maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
